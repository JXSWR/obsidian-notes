# 2026-09-02 工作日志

## 记忆同步：vault 架构大变更（用户用 workbuddian 插件干了两天活）

读 `CODEBUDDY.md` + `.codebuddy/rules/SYSTEM.md`，并**实际核实文件系统**后，重写了
`~/.workbuddy/memory/topics/obsidian-vault.md`，在 `~/.workbuddy/MEMORY.md` 新增"活跃线状态修正"小节。

### 核实到的硬事实
- Claude Code 残留清除：`.claude/`、`CLAUDE.md`、`.mcp.json` 均已不存在
- 治理层 = `d:\My knowledge vault\.codebuddy\`（rules/SYSTEM.md + agents×3 + commands/review.md + settings.json）
- 用户级 `~/.codebuddy/rules/obsidian-vault.md` 已降级为 418B 位置指针，不再重复系统内容
- `70_Blender专区/` 已删除 → Blender 孙悟空线确认结束
- Obsidian 插件只剩 `workbuddian`、`remotely-save`；MCP 全部废弃

### 与用户表述有出入的两点（已按事实记录）
1. **双位置架构并未取消** —— `D:\repos\项目实践` 仍存在且有内容（`产品开发/cimatron二次开发/` git 仓 77 个非 git 文件、`淬锋应用网站/`；`项目开发/` 为空）。收敛的是**权威描述**：SYSTEM.md 单点权威 + 用户级只做指针。
2. **learning-coach 不是消失，是落地** —— `.codebuddy/agents/learning-coach.md` 仍在且被 SYSTEM.md 列为 sub-agent；过时的只是"评估中"这个状态。

### 顺带发现（未处理，留待用户确认）
- Cimatron 代码仓实际在 `D:\repos\项目实践\产品开发\cimatron二次开发\`，与跨项目记忆里的路径描述不一致，需择机校正。
- 云端注入 profile 里的"其他活跃线"三条已过时，云端改不了，已在本地 MEMORY.md 加覆盖段。

---

## 第二轮：用户下达 6 条变更，落地清理

### 已执行
1. **删除活引用中的 Blender 条目**：
   - `d:\My knowledge vault\.codebuddy\rules\SYSTEM.md` 权限矩阵第 34 行（`70_Blender专区/`）已删
   - `~/.workbuddy/skills/obsidian-output-routing/SKILL.md` 同一行已删
   - 用户级 `MEMORY.md` 与 `topics/obsidian-vault.md` 改写为不出现该词（改用编号指代）
2. **顺手修了 obsidian-output-routing skill 的 3 处过时**：`.claude/` → `.codebuddy/`；
   "WorkBuddy 无钩子、靠自觉" → 改为 deny 硬拦截已实测生效；`CLAUDE.md` → `CODEBUDDY.md`
3. **SYSTEM.md 措辞**："用户级镜像见" → "用户级位置指针见（只引用，不重复本文件内容）"
4. 记忆更新：MEMORY.md 新增 4 条（活跃线废止/完成、Claude Code 切断但 CLI 仍在用、
   WB 项目级配置加载关系实测、vault GitHub 仓信息）；topics/obsidian-vault.md 全面重写（新增
   「WB 配置加载关系」「Git 仓库」两节）

### 核实结论
- git：4 条提交、master、0 领先 0 落后、分支与 worktree 已清 —— **但工作区有 2 个未提交文件**
  （`10_手册/工具速查.md` 已改、`10_手册/术语速查.md` 未跟踪），未擅自提交
- 全盘扫描：无任何 `.blend` 工程文件；vault 内亦无该词残留
- 52 处含 blender 字样的文件中，**绝大多数不可动**：`artifact-index/`、`changes-detail/`、
  `usage-log.json`、`mcp-approvals.json` 是 WB 机器审计数据；`connectors-marketplace/connectors.json`
  里的 blender 是"便携榨汁机"商品（误伤）；`~/.workbuddy/memory/4c39e9a6-*_memory.md` 是云端
  profile 快照（改了会被服务器覆盖）

### 三处与用户表述有出入（已按事实记录）
1. `OBS录屏指南.md` 在 `40_自媒体\平台\B站\`，不在 `10_手册\`（内容确认无误）
2. `USER.md` 里**没有** Blender 字样，也没有"活跃线"列表 —— 活跃线④ 在云端 profile 里
   （顺带：USER.md 有两处过时——"已整合工具：Claude Code"、"对 Claude Code 依赖较深"，未擅改，已提示）
3. "远程与本地完全一致"只在**提交历史**层面成立，工作区层面不成立

### 待用户定夺
- `~/.workbuddy/mcp.json` 里的 blender MCP 服务器是否一并移除（当前工具列表仍有该组工具在跑）
- vault 的 2 个未提交文件是否提交

---

## 第三轮：用户拍板，两项清理已执行完毕

### 1. Blender MCP 彻底清理（用户选「配置 + venv 全部删除」）
1. 备份 `mcp.json` → `C:\Users\BEER\AppData\Local\Temp\mcp.json.bak-20260902`（**保留未删**）
2. 从 `mcp.json` 移除 blender 段 → 只剩 `solidworks`（本就 `disabled: true`，且其 venv 早已不存在）
3. **删 venv 踩坑**：首次 `rm -rf` 被 safe-delete **fail-closed 拒删**（genie-trash 报 aborted）。
   诊断：4784 个文件中仅 `Scripts\blender-mcp.exe` 被锁（MCP 进程占用）。
   解法：`taskkill /F /IM blender-mcp.exe`（终止 2 个 PID）→ 重试 `rm -rf` 成功，释放 C 盘 87MB。
   `envs/` 现只剩 `default`。→ 已作为跨项目经验写入用户级 MEMORY.md

### 2. Git 提交并推送（用户选「提交并推送」）
- 实际提交 **3 个**文件而非 2 个：除两个手册外，加上 `.codebuddy/rules/SYSTEM.md`
  （我删 Blender 条目 + 改措辞的改动也在 vault 内且被跟踪，不提交则工作区长期脏）
- commit `94b84b2`，+200/-96，新建了 `10_手册/术语速查.md`
- `git push origin master` **一次成功，SSH 无需代理**
- 最终：`git status` 干净，`0 领先 0 落后` —— "远程与本地完全一致"至此才真正成立

### 3. USER.md 两处过时已修
- "已整合工具：Claude Code、..." → 改为 WorkBuddy 主力，并加切断说明（CLI 仅用于 cimatron）
- "对 Claude Code 依赖较深" → 改为已由 WorkBuddy 接管，仅 CLI 保留

### 剩余待办
- `10_手册/工具速查.md` 里写"Claude Code = VS Code 扩展，用于 cimatron 的 C#"，
  与用户第 2 条"CLI 还在用"表述不一致（不冲突，可择机统一）
- 非标设计 `05_项目案例库/` 仍未创建
