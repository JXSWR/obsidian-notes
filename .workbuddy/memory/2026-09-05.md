# 2026-09-05 工作日志

## 早间①：项目长期记忆 MEMORY.md 全面更新（用户指令"更新这个文件"）
- 触发：用户手动删除 `E:\模型文档\案例机构库\`，记忆里指向该路径的整节（9-03 迁移记录）作废。
- 先核实再改（未凭印象）：
  - `E:\模型文档\` 确认已删除；**`E:\非标设计\` 为唯一真源**，5 项目齐全 = **3843 文件 / 2.59 GB**（001 813M·1283 / 002 734M·1070 / 003 245M·429 / 004 473M·824 / 005 328M·237）+ 顶层 `00_案例总览.md`、`案例索引.md`、`_通用参考资料`。
  - vault 实测 **850 文件 / 291.1 MB**，md **194 个 / 0.67 MB**。
  - xlsx：全库仅 3 个，**04_查询库 最强非标自动化计算表格.xlsx（31.3 MB，MD5 5bec8f44，唯一副本）**、时序图表格、60_存档 BOM 表 → **旧待办"三份 xlsx 去重"已自然闭环**（62.5 MB 已释放），该条降为已闭环记录。
  - `_原始资料` 只剩 4 张挡圈 png（无 xlsx）。
- 改写六处：一 基本盘 / 二 外置真源（含旧路径作废警告）/ 三 配置（xlsx 零备份明细）/ 六 待办（重排，新增 05_项目案例库删除、03_计算校核 文本残留、00 全局 02 表断链三项 A 级）/ 七 教训（du 不可靠）/ **新增八节：非标设计知识库架构（9-04 定型）**。
- 限额控制：首版 4328 字符超限 → 三轮压缩到 **2999 / 3000**，8 章节完整、U+FFFD 0。

## ⚠️ 本次发现的两个事实（未处理，待定）
1. **`05_项目案例库` 实为 253 MB / 594 文件**（不是昨晚估的 35 MB）。它占 vault 总体积 **87%**，被 git 忽略、无版本保护；内容是 `E:\非标设计` 全集的子集。确认覆盖后删除可让 vault 291 MB → 约 38 MB。
2. **`.workbuddy/memory/` 整个目录未进 git**（`git ls-files` 命中 0）→ 长期记忆与流水日志**无版本备份**，误删不可恢复。
3. 遗留：全库 8 处纯文本仍写旧目录名 `03_计算列表` + `00_全局/索引.md` 03 段标题漏改（昨晚 36178e5 未覆盖）。

## 早间②：记忆层纳入 Git（commit 8abcbbe）—— 上述第 2 条已闭环
- 用户问「长期记忆是 Obsidian 还是 WorkBuddy 文件」→ 实测结论：**纯 WorkBuddy 数据，三不沾**（Obsidian 不索引点开头隐藏目录；`.gitignore:76` 排除；`.stignore:9` 排除，注释"frequently changing, conflict-prone, machine-specific"）。
- 对比方案 1（纳入 git）/ 2（外拷 D:\备份）后用户选 1。关键论据：方案 2 致命伤是**同盘（D:\备份 与 vault 同盘）+ 依赖人主动执行**（违背 9-04「积小步提交」教训）。
- **踩坑（实测，非推测）**：`.gitignore` 排除整目录后，**git 不再递归进去应用否定规则** —— 直接加 `!.workbuddy/memory/` **无效，`check-ignore` 仍命中第 76 行**。必须改成通配符写法：`.workbuddy/*` + `!.workbuddy/memory/`，实测生效（`git status` 出现 `?? MEMORY.md`）。
- 执行：二进制改写 .gitignore 保行尾（该文件实测为**纯 LF，CRLF=0**）→ git diff 仅 1 行变更；纳入 5 文件（MEMORY.md + 09-02~09-05 日志），`.workbuddy/outputs/`（13 脚本 + opendoc.bin）继续忽略。
- 敏感扫描：仅命中 SSH 钥匙**文件名** `id_ed25519`（非私钥内容）。**仅本地 commit，未 push** —— 是否推远端留给用户定。
- 顺带：`00_全局/索引.md` 的 M 是 Obsidian 行尾幽灵差异，`git add` 后暂存区内容为空、工作区归零（同 9-04 公式索引现象），无需提交。

## 早间③：敏感扫描 + 推送 32 个积压提交（C 方案，远端已同步）
- 背景：推前查远端发现本地 **ahead 32**——9-04 整天重组成果全在本机，异地容灾为零。用户选「先扫后推」。
- **扫描踩坑（假阴性）**：`git ls-files` 默认把中文路径转义成八进制（`20_\344\270\223...`），Python 按该字符串 `os.path.isfile` 判 False → **239 个文件只扫到 20 个，报告"未发现敏感内容"是假结论**。改用 `git ls-files -z`（NULL 分隔不转义）后 239/239 全覆盖。**教训：扫 git 中文路径文件必须加 `-z`。**
- 扫描结果：① 未推送 diff（6633 行 / 282 KB）**零命中**；② 全库 239 文件命中 8 处——6 处 `git@github.com`（SSH remote 标准格式，非敏感）、1 处二进制 jpg 误报、**1 处真个人信息：`10_手册/06_GitHub-SSH连接配置指南.md:159` 的 QQ 邮箱 601024079@qq.com**。
- **邮箱清理无意义**：git grep 确认它**早就在 origin/master 远端历史里**（首次出现 commit `642db22`，旧文件名时期推送的）。私有仓仅本人可见，改历史成本远大于收益 → 保留，不 filter-branch。
- 确认**无私钥/无 API key**（`git grep "PRIVATE KEY"` 零命中）。
- 推送：`b8f3707..be01876 master -> master`，32 提交全部上远端，**SSH 直连成功无需代理**（印证 9-03 记录）。最终 `master...origin/master` 无 ahead，工作区干净，`.git` 425 MB。

## 早间④：MEMORY.md 按「给 AI 看的目的」精简（2999 → 2312 字符）
- 用户提问直击本质：「长期记忆是给 AI 看的，那么给 AI 看的目的是什么？我固化了的信息是否意味着记录无效」。
- **确立判据（写入文件头部）**：价值 = 重新发现的代价 × 不知道的后果 ÷ 漂移速度。四个目的按价值排：防踩坑 > 防漂移 > 防重复劳动 > 行为纪律。**不记**：已固化进 vault 文档的、扫一眼就得到的、会漂移的数字、一次性任务的技巧。
- **对「固化了是否无效」的回答**：是的，分三类——固化进 vault 文档的删（两分判定已在 `00_库索引` 顶部、机构模板文件在位）；固化进磁盘结构的删（三级导航、X索引命名，扫目录即见）；固化进流程习惯的不记。
- 执行：8 节 → 5 节，删 687 字符。
  - 删：vault 盘面数字（850文件/291MB）、`git remote -v` 可得的仓信息、`.stignore`（文件本身在 vault 可读）、插件列表（扫 `.obsidian/plugins`）、已闭环的 xlsx 去重条目、PDF 归属规则与 lookahead 正则（一次性迁移技巧）、架构节里已固化的目录序列/三级导航/两分判定/机构模板、成熟度百分比数字。
  - 留：vault 外事实（E 盘真源、`D:\repos\项目实践`）、⚠️旧路径已删（最高价值防漂移）、云端 profile 作废、积小步提交、Python 统计、`-z` 教训、gitignore 大小写、xlsx 零备份、矢量图说明、po-v4 前置条件、填肉优先级 01>03>02>07。
  - 新增：memory 已纳入 git 的写法说明（`.workbuddy/*` + `!.workbuddy/memory/`）。
- 校验：2312/3000、5 章节、U+FFFD 0；9 个应删项确认清零，10 个应留项确认在位。
- 未擅自做：矢量图教训未固化进 `04_查询库/README.md`（用户未明确批准，故仍留在记忆里）。

### CODEBUDDY.md 纠正：必读/按需 位置对调（用户看截图质疑后确认反了）
- 原结构错误：`MEMORY.md` 标「开工前必读」、`SYSTEM.md` 只是普通链接，且两者都塞在「按需查询」区；「开工前门禁」第 1 条又重复要求读 MEMORY.md → **双重定义 + 层级错位**。
- 判断依据（读 SYSTEM.md 原文后）：SYSTEM.md 是全局规则（双位置架构/目录权限矩阵/检索路由/全局约定），**每次操作都要遵守**，不读就按错行为走（例：不知 `_原始资料` 硬拦截就去写）；MEMORY.md 是项目特定事实；设计/CNC 知识库才是真按需。
- 修正后：① 新增「## 全局规范（开工前必读）」，SYSTEM.md 在前、MEMORY.md 在后；② 「按需查询」只剩设计/CNC 两库；③ 删除「双位置架构（做项目时必读）」小节（合并进 SYSTEM.md 引用）；④ 开工前门禁由 4 件事减为 3 件，删掉重复的「读 MEMORY.md」。
- 未动 `SYSTEM.md`（按规则它是唯一权威，只改 CODEBUDDY.md 的指向）。文件 38→33 行，LF 保持不变（二进制写入），治理层其他 5 文件无重复定义。

### 记忆体系盘点：三层结构 + 清理 8 处过期残留
- **记忆三层**（实测确认）：① 云端 profile，服务端生成、每轮自动注入，本地缓存在 `C:/Users/BEER/.workbuddy/memory/<uuid>_memory.md`（12k 字符，Version 34）；② 用户级跨项目 `C:/Users/BEER/.workbuddy/MEMORY.md`（2.7KB，自动注入）+ `memory/topics/`×5、`archive/`×2 冷库（不注入，需 ls）；③ **项目记忆** `<工作空间>/.workbuddy/memory/MEMORY.md` + 每日日志，**不注入必须主动读**。
- **项目记忆是懒创建的**：`C:/Users/BEER/.workbuddy/projects/` 有 42 个会话槽位，但只有 11 个目录真落了 memory。
- **全盘 11 处 memory → 清理后剩 3 处**：`D:/My knowledge vault`（vault，活跃）、`D:/workspace/workbuddy`（cimatron，停在 9-02）、`C:/Users/BEER/.workbuddy/memory`（云端缓存+用户冷库，非项目记忆）。
- **清理的 8 处**：`C:/Users/BEER/WorkBuddy/<时间戳>/.workbuddy/memory/` ×8，11 文件 / 51.3 KB，走回收站（已验证 11 个全在回收站可还原，10:54–11:00）。
- ⚠️ **差点误删的坑**：那 8 个会话目录里塞着非记忆内容——`2026-07-23-17-19-31/项目案例库` **3838 文件 / 2.93 GB**、`2026-08-02-13-03-32/my-video` 920 文件 / 111.7 MB，另有 3 份** vault 内无同名的孤本 md**（Cimatron与机床操作_整理版 / 非标项目开发流程_整理版 / 道德经）。**只能删目录内的 memory/ 子目录，绝不能整目录删**。
- **两个待决发现**：① C 盘 2.93 GB 案例库顶层是 `11_/12_/13_` 旧编号，而 E 盘真源是 `001_/002_` 新编号（3860 文件 / 2916 MB）→ **是迁移前旧副本，确认后可删释放 C 盘 2.93 GB**（文件数差 22、体积差 19MB，需用户确认）；② **cimatron 记忆与代码分离**——记忆在 `D:/workspace/workbuddy`，代码仓在 `D:/repos/项目实践/产品开发/cimatron二次开发`（有 .git 无 .workbuddy，且 **git remote 为空 = 零远端备份**）。

## 界面删除 ≠ 删文件（验证 + 固化 2364cb6）
- 用户在 WorkBuddy 右侧预览面板删掉 md 展示卡片，担心文件丢失 → 查 `git status`：0 项删除，239 个跟踪文件（md 193）全在位 → **md 预览面板是只读渲染，删的是展示卡片，不碰磁盘**。今后遇同类询问先跑 `git status` 验证再回答，别猜。
- 同批发现 `.workbuddy/memory/MEMORY.md` 有未提交改动：5 处段落空行规范化 + **用户手动删掉 2 条**（① 计算表格公式以矢量图嵌入、EMF 149 + WMF 53 的教训；② `scripts/po-v4.*` 需新建 DeepSeek key 的 P0 遗留）→ 按用户删后状态提交 `2364cb6` 并推送 master；旧版仍在 HEAD（`b8461c7`），需要可捞回。
- **连带影响**：矢量图教训已从记忆移除，此前提议「固化进 `04_查询库/README.md`」一事无记录可依，如仍需须重新论证。

## 目录改名同步 + 首次全库死链扫描（947645f）
- `03_计算列表`→`03_计算校核`：实际 **9 处**（MEMORY.md 记 8 处，漏了 `00_全局/索引.md` 的段标题），已全量替换、Grep 验证零残留，`947645f` 已推送。
- SYSTEM.md + CODEBUDDY.md 新增「改名/移动后必查」规则：① Grep 搜旧名 ② 全库 wikilink 死链扫描。
- **首次全库死链扫描**：183 md / 695 链接有效 / **21 处真死链（12 个目标）** / 30 处 TODO 占位符（有意保留，不算死链）。
- **扫描脚本三个坑**（已写进 SYSTEM.md，勿重踩）：`[[A\|别名]]` 表格转义须按 `(?<!\\)\|` 拆分；带扩展名的链接（`.xlsx`/`.png`）须用「含扩展名 + 不含扩展名」两套 key 匹配；`TODO:` 开头的是占位符需排除。第一版脚本因这三点跑出 150 处假阳性（真值仅 21）。
- **最严重发现**：`标准件与通用知识` 被 5 处依赖（轴系设计 §五、材料牌号对照 §六、标准件速查等），**文件却不存在** → AI 检索到这 5 处全是死路。处置待用户定。
- 另核实：两个 xlsx 计算表格（含 31 MB 唯一副本）**均在 04_查询库 完好**，此前扫描报缺失是脚本误判。

## 死链清理收尾（06dc1d9）：真死链 21 → 0
- 建 3 个占位页（只给骨架，**零编造**）：`01_机构库/丝杆模组.md`、`01_机构库/竖直丝杆模组.md`（套机构模板五段）、`04_查询库/标准件与通用知识.md`（6 章节骨架；**§四 引用处无线索，留空标「待定」不猜**）。
- 8 处已删页引用改「~~删除线~~ ⚠️待重建」纯文本（6 处在 `00_全局/索引.md` 02 表，2 处在 `03_计算校核` 的「参考」段）：既消除死链又保留信息，重建后加回链接即可。**这是处理已删页引用的标准做法，以后照此办理。**
- 剩余 4 处扫描命中均为有意设计，不必处理：`30_思想花园/索引.md` 的 `xxx`×2 在 **HTML 注释块**内；`40_自媒体/素材片段/README.md` 的 `片段名称` 是模板示例文字；`00_收件箱/处理记录.md` 的「对话识图自动化方案」是 8 月归档历史记录。
- **指标变化**：抗改名 4→9、零断链 5→9（唯一归位 8 / 浅层可达 9 / 缺口可见 9 / 流程同构 9 不变）。
- 扫描脚本在 `D:\tmp\check_links.py`（vault 外，因 vault 只放 .md）；SYSTEM.md 规则要求每次改名后跑它，**需给它找个固定位置**，否则 /d/tmp 被清就失效。
