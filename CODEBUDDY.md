# CODEBUDDY.md

> WorkBuddy 项目总纲（已从 Claude Code 迁移完成并彻底切换）。

## 全局规范（开工前必读）

- 所有输出用中文。代码标识符和 git 命令除外。
- **系统总纲** → @.codebuddy/rules/SYSTEM.md — 双位置架构、目录权限矩阵、检索路由、全局约定。不读就按错行为走（如不知道 `_原始资料` 是硬拦截就去写）
- **项目长期记忆** → [.workbuddy/memory/MEMORY.md](.workbuddy/memory/MEMORY.md) — vault 项目事实、待办、教训。**不自动注入，不读必漏**

## 按需查询

- **设计知识库** → [总索引](20_专业领域/01_非标设计/00_全局/索引.md) — 方案设计时查阅
- **CNC 知识库** → [总索引](20_专业领域/02_数控编程/00_全局/索引.md) — 数控编程时查阅

## 压缩指令

/compact 时保留：当前任务目标、关键决策及理由、未完成的待办。
丢弃：搜索中间结果、已解决的报错详情、已读取过的文件全文。

## 开工前门禁

非平凡任务（多文件、多方案、改结构、设计/编程）动手前先做 3 件事（读 SYSTEM + MEMORY 已在全局规范要求，此处不重复）：
1. 复述我理解的需求
2. 列出待定决策点
3. 等你确认后再执行

琐碎任务（改错字、单文件小改、指令明确）直接做，不必问。

## 常见陷阱

- ❌ 没核实就报完成 — 先跑命令验证，再说结果
- ❌ 没确认需求就自由发挥 — 按门禁流程先对齐再动手
- ❌ 改名/移动后直接提交 — 旧名残留 + 死链各扫一遍再固化（见 SYSTEM.md「改名/移动后必查」）
